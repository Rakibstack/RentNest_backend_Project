enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
}

enum PropertyStatus {
    AVAILABLE
    UNAVAILABLE
}

enum RentalRequestStatus {
    PENDING

    APPROVED

    REJECTED

    ACTIVE

    COMPLETED
}
enum PaymentProvider {
  STRIPE
  SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
}
